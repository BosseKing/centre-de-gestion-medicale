# Docker

Ce dossier contient tous les fichiers relatifs à la configuration Docker du projet SOA "Centre de gestion médicale".

## Contenu
- `docker-compose.yml` : orchestration des services
- `Dockerfiles/` : Dockerfiles pour chaque service
